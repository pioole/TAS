TAS project - short version readme.

prerequisites:

    python2.7.x installed
    python numpy module installed (tested with numpy-1.11.2)
    python matplotlib module installed (tested with matplotlib-1.3.1 and TkAgg backend)

running:

    $ python NodeCluster.py

